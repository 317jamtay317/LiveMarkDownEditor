namespace UI;

/// <summary>
/// The WPF application. Startup is owned by <see cref="Program"/>, which resolves the main window
/// from the host's service provider and runs it. This class only carries the application-level
/// resources declared in <c>App.xaml</c>.
/// </summary>
public partial class App : System.Windows.Application;
