namespace UI.ViewModels;

/// <summary>Which navigation panel a Side Dock tab hosts (INV-046).</summary>
public enum SideDockTab
{
    /// <summary>The Folder Panel — the open Folder Workspace's Folder Tree.</summary>
    Folder,

    /// <summary>The Navigation Panel — the Active Session's Outline.</summary>
    Navigation,
}
